# 测试与验收记录

## 记录日期

- 2026-03-17
- 2026-03-26

## 当前版本已验证项

### 1. 工程构建

- 前端 `npm test` 通过
- 前端 `npm run build` 通过
- 后端 `mvn -q test` 通过
- 后端 `mvn -q -DskipTests package` 通过
- `docker compose up -d --build diagnosis-service backend` 通过
- `docker compose up -d --build diagnosis-service backend frontend` 通过
- 前端容器首页 `http://127.0.0.1/` 返回 `200`

### 2. 功能闭环

- 账号登录、角色权限与受限路由已存在自动校验
- 智能问答支持来源卡片、会话列表和历史回放
- 知识图谱支持可视化、实体搜索、关系详情和关联跳转
- 病害识别支持 YOLO 模型优先加载，并保留降级模式
- 农户、门店、技术员三类工作台与主要页面已纳入当前实现
- 解决方案推荐、门店方案、求助流转链路已形成可演示闭环
- 技术员可通过文档、图谱、评测和系统状态页持续优化平台
- 后端已补充写操作审计日志，覆盖登录、登出、文档写入、反馈提交和系统初始化等关键操作
- 已完成用药剂量安全校验评审，当前版本明确定位为建议展示，不宣称提供精确剂量处方
- 已修复识别服务 YOLO 推理路径中的 tensor 布尔判断异常，后端 `/api/diagnosis` 已恢复返回 `ultralytics-yolo`

### 3. 自动化测试覆盖

- 后端 smoke test 已覆盖认证、评测、文档上传、满意度问卷、健康检查和角色权限
- 后端 `CollaborationServiceTest` 已覆盖推荐方案、提交求助、门店收件箱、状态更新和持久化重载
- 后端 `FeedbackServiceTest` 已覆盖满意度提交、统计汇总和本地状态迁移到 MySQL 的回归场景
- 前端访问控制测试已覆盖技术员、农户、门店的关键权限判断
- 前端已补充三角色默认首页、导航入口、页面元信息与关键状态配置校验

### 4. 2026-03-26 全量回归与主链路彩排

- 2026-03-26 已重新执行前端 `npm test`
- 2026-03-26 已重新执行前端 `npm run build`
- 2026-03-26 已重新执行后端 `mvn -q test`
- 2026-03-26 已重新执行后端 `mvn -q -DskipTests package`
- 2026-03-26 已通过 Docker 启动 `diagnosis-service`、`backend`、`frontend`
- 2026-03-26 已执行 `scripts/rehearse-main-flow.ps1`
- 彩排报告目录：`reports/rehearsal/main-flow-20260326-141644.json`
- 彩排结果：健康检查为 `healthy`，Neo4j / Milvus / Ollama / diagnosis 全部 `connected`
- 彩排结果：识别接口已返回真实 `ultralytics-yolo` 结果
- 彩排结果：问答接口返回来源卡片，当前样本命中 `4` 条来源
- 彩排结果：已成功命中 `plan-demo-anthracnose`，并完成“农户提交求助 -> 门店更新为 contacted -> 技术员读取系统设置”的闭环
- 联调期间已修复推荐方案与高频病症看板的排序比较器，当前趋势第一项已恢复为 `炭疽病`
- 联调期间已补充协同状态脏数据自愈逻辑，旧的占位病症标签在服务重启后会自动回填为方案标签
- 已新增 `scripts/rehearse-main-flow.ps1` 作为后续答辩前复跑入口

### 5. 当前基线测量

- 2026-03-17 已执行 `scripts/measure-api-baseline.ps1`
- 报告目录：`reports/validation/20260317-135124/`
- 问答接口：预热 1 次后，对 5 个不同问题样本测量，平均 `4159.4 ms`，满足平均 `< 5s` 目标
- 图像识别接口：预热 1 次后测 5 次，平均 `76.65 ms`，满足 `< 3s` 目标
- 当前识别基线基于真实 `ultralytics-yolo` 路径，不再是后端兜底结果
- 问答链路已增加热点问题短时缓存、上下文裁剪和 LLM 超时保障，避免长尾请求明显拖慢平均值
- 2026-03-17 已执行 `scripts/measure-api-concurrency.ps1`
- 并发报告目录：`reports/validation/20260317-204935/`
- 问答链路：`50` 并发、`50` 请求全部成功，成功率 `100%`，总耗时 `11388.93 ms`，吞吐 `4.39 req/s`
- 并发压测结果已覆盖需求文档中的 `50+` 并发用户目标验证
- 2026-03-17 已执行 `scripts/measure-service-stability.ps1`
- 稳定性报告目录：`reports/validation/20260317-210017/`
- 连续 `5` 分钟、每 `10` 秒一轮，共 `30` 轮健康/问答/识别巡检全部成功，可用率 `100%`
- 2026-03-26 已执行 `scripts/measure-service-stability.ps1 -DurationMinutes 30`
- 稳定性报告目录：`reports/validation/20260326-150312/`
- 连续 `30` 分钟、每 `15` 秒一轮，共 `119` 轮健康/问答/识别巡检全部成功，可用率 `100%`
- 当前稳定性样本已覆盖 30 分钟巡检口径，可作为“>99% 可用性目标”的更强证明

### 6. 可演示性

- 仓库已包含 `models/yolov8-litchi.pt`
- Docker 与本机模式都保留了图谱、问答、识别的降级保障路径
- 当前前端已形成农户、门店、技术员三角色协同演示链路
- 后端打包产物已清理掉额外的 `reload4j` / 冗余 logging provider 冲突依赖

### 7. 验收材料补充

- 已形成 `docs/最终验收摘要.md`，用于统一说明当前完成度、已闭环证据与剩余线下补测边界
- 已形成新的浏览器与移动端验收清单，覆盖角色首页、协同页面与权限拦截

## 当前版本待线下补测项

### 1. 兼容性验证

- Chrome、Firefox、Edge、Safari 实机浏览器验证
- 手机端页面实机验证
- 已细化 `docs/浏览器与移动端验收清单.md`，包含三角色首页、协同链路、权限拦截与移动端布局检查项

## 结论

- 当前版本已经达到“可构建、可测试、可答辩演示、核心功能闭环”的状态
- 截至 2026-03-26，代码回归、Docker 联调和三角色主链路彩排均已补齐
- 若要完全对齐需求文档中的非功能目标，还需要补充浏览器与移动端兼容性验证记录
