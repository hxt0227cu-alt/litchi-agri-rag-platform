# ADR-003：业务层通过统一模型网关切换推理引擎

- 日期：2026-07-22
- 状态：已采纳

## 决策

保留现有 LLMService 作为模型网关。开发环境使用 Ollama 协议，云端通过 OpenAI-compatible 协议连接 vLLM，业务与 Agent 不感知推理引擎差异。

## 权衡

该实现比引入完整模型网关产品轻量，足以完成模型对比和故障降级。后续出现多租户配额、动态路由和多模型计费需求时，再独立拆分 Gateway 服务。
