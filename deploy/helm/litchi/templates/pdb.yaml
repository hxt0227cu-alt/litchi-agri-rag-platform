apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "litchi.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "litchi.name" . }}
